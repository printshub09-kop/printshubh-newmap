services:
  - type: web
    name: printshubh-bhunaksha
    env: node
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
